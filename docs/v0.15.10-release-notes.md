# masuidrive-kbd v0.15.10

- Foldなどのジェスチャーナビゲーション端末で、最下段キーがOSのホームジェスチャー領域へ重なる問題を修正しました。
- `systemBars`が一時的に0を返す場合も直前の安全領域を維持し、`systemGestures`の下端も含めます。
- 日本語変換で横スクロールした後の候補をタップすると、隣の候補が確定する問題を修正しました。
- 候補は表示時のMozc candidate IDを`SUBMIT_CANDIDATE`へ渡し、選んだ文字列をそのまま確定します。

## 検証

- API 36.1 AVDの実Mozcで「にほんね」の候補「2本ね」が完全一致で確定することを確認しました。
- 単一文節、複数文節、次単語予測、学習、履歴削除を含む実Mozc connected test 9件を確認しました。
- 1499×1680、800dp幅相当、Dark、Dual Flick、高さ「大」で最下段キーがホームジェスチャー領域の上へ収まることを画面確認しました。

Fold実機固有のWindowInsets通知順はエミュレータで完全には再現できないため、実機での最終確認は継続します。

# masuidrive-kbd v0.15.21

- 変換中でないEnterを上へフリックするとPaste、下へフリックするとCtrl+Jを送るようにしました。
- Enterの待機中は上に`C-j`、中央に`Enter`、下に`paste`を表示します。
- 記号レイヤー3行目のTabを右端に置きました。

ARM64向けのdebug署名APKです。Android 9（API 28）以上で動作します。

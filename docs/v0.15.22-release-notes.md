# masuidrive-kbd v0.15.22

- 未割当方向へ指がずれた場合も、選択表示を変えず、中央タップと同じ文字を一度だけ入力するようにしました。
- テンキーの`-`に左`+`・上`/`・右`*`・下`,`、`.`に左`,`・右`=`の方向ラベルを表示します。`.`の上・下にはラベルを表示しません。
- テンキー記号の方向ラベルは、選択時にEnterと同じ操作体系で強調されます。

ARM64向けのdebug署名APKです。Android 9（API 28）以上で動作します。
